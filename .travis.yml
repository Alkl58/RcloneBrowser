git:
  depth: 1

matrix:
  include:

    - os: linux
      language: cpp
      sudo: false
      dist: jammy
      addons:
        apt:
          packages:
            - qt6-base-dev
            - qt6-multimedia-dev
            - libgl1-mesa-dev

      script:
        - mkdir build && cd build
        - cmake ..
        - cmake --build .
        
